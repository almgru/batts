[Unit]
Description=batstat daemon

[Service]
Type=simple
ExecStart=batstat daemon --interval-in-seconds=60 --log-directory=$XDG_DATA_HOME/batstat
ExecStop=killall --user --wait batstat

[Install]
WantedBy=default.target
